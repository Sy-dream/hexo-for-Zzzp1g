# Hexo 博客模板中文使用说明

这是一套已经配置好的 Hexo + Butterfly 博客模板，适合直接拿来改成你自己的博客。

## 1. 先准备环境

请先安装下面两个软件：

- Node.js
- Git

安装完成后，打开 PowerShell，执行下面命令检查是否安装成功：

```powershell
node -v
git --version
```

## 2. 获取模板

把模板仓库克隆到本地：

```powershell
git clone https://github.com/Sy-dream/hexo-for-Zzzp1g.git
cd hexo-for-Zzzp1g
```

然后安装依赖：

```powershell
npm install
```

## 3. 本地运行

第一次运行前，建议先清理并重新生成：

```powershell
npx hexo clean
npx hexo generate
npx hexo server
```

浏览器打开：

```text
http://localhost:4000
```

只要终端里这个服务没关，网页就能本地预览。

## 4. 第一次要改哪些地方

优先修改这几个文件。

### `_config.yml`

主要改这些字段：

- `title`: 博客标题
- `description`: 博客描述
- `keywords`: 关键词
- `author`: 作者名
- `url`: 你博客最终访问地址
- `deploy.repository`: 你自己的 GitHub Pages 仓库地址

示例：

```yml
title: Alice Blog
description: My notes and daily record
keywords: "blog,hexo,butterfly"
author: Alice
url: https://yourname.github.io

deploy:
  type: git
  repository: git@github.com:yourname/yourname.github.io.git
  branch: main
```

### `_config.butterfly.yml`

主要改这些内容：

- 首页副标题 `subtitle.sub`
- 社交链接 `social`
- 侧边栏作者说明
- 公告内容
- 评论系统配置（如果你要开评论）

例如你至少可以先把 `social` 里的占位内容换掉。

### `source/about/index.md`

这里是“关于我”页面，改成你自己的介绍。

### `source/_posts/hello-template.md`

这是示例文章。你可以直接删掉，或者改成你自己的第一篇文章。

### `source/_data/link.yml`

这里是友链数据。如果你暂时不用友链，可以先不动。

## 5. 怎么写新文章

新建一篇文章：

```powershell
npx hexo new "我的第一篇文章"
```

生成的文件会出现在：

```text
source/_posts/
```

文章写完后，再运行：

```powershell
npx hexo clean
npx hexo generate
npx hexo server
```

刷新浏览器就能看到效果。

## 6. 图片放哪里

常用做法是把图片放在：

```text
source/img/
```

例如放一张 `test.png`，文章里可以这样写：

```markdown
![示例图片](/img/test.png)
```

## 7. 发布到 GitHub Pages

### 第一步：创建你自己的仓库

建议在 GitHub 新建一个仓库，例如：

```text
yourname.github.io
```

如果你想把博客直接挂在 GitHub Pages 根域名下，仓库名通常就是这个格式。

### 第二步：修改 `_config.yml`

把下面两项改成你自己的：

- `url`
- `deploy.repository`

例如：

```yml
url: https://yourname.github.io

deploy:
  type: git
  repository: git@github.com:yourname/yourname.github.io.git
  branch: main
```

### 第三步：执行部署

```powershell
npx hexo clean
npx hexo generate
npx hexo deploy
```

部署完成后，到 GitHub 仓库设置里确认 Pages 是否启用。

## 8. 常用命令

安装依赖：

```powershell
npm install
```

本地预览：

```powershell
npx hexo server
```

清理缓存：

```powershell
npx hexo clean
```

重新生成静态页面：

```powershell
npx hexo generate
```

发布到远程：

```powershell
npx hexo deploy
```

新建文章：

```powershell
npx hexo new "文章标题"
```

## 9. 常见问题

### `npm install` 失败

先确认 Node.js 安装正常，再重新打开 PowerShell 执行一次。

### 本地网页打不开

先确认你有没有执行：

```powershell
npx hexo server
```

然后检查地址是不是：

```text
http://localhost:4000
```

### 部署失败

通常是下面几种原因：

- `deploy.repository` 没改成你自己的仓库
- GitHub 还没登录
- SSH key 没配好
- 仓库名和 `url` 不匹配

## 10. 建议的使用顺序

建议按这个顺序操作：

1. 克隆模板
2. 执行 `npm install`
3. 执行 `npx hexo server`
4. 修改 `_config.yml`
5. 修改 `_config.butterfly.yml`
6. 修改 `about` 页面
7. 写第一篇文章
8. 新建自己的 GitHub Pages 仓库
9. 修改 `deploy.repository`
10. 执行 `npx hexo deploy`

如果只是先本地写博客，可以先不部署，等内容改得差不多再发到 GitHub。
